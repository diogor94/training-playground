pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	enviroment {
		BUILD_CONFIGS = '''
			name: "testValue",
			command: "echo 'Hello world!'"
		'''
	}
    stages {
        stage('build') {
            steps {
                script {
                	def buildConfigs = []
                	buildConfigs.each{ buildConfig ->
                		sh "echo '$buildConfig.name' && $buildConfig.command"
					}
                }
            }
        }
    }
}
