def userInput (text = '') {
	
}

pipeline {
	parameters {
		choice(name: 'CHOICE', choices: ['build','test'])
	}

	agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	stages {
		stage ('build') {
			steps {
				echo 'build'
			}
		}
		stage ('test') {
			steps {
				echo 'test'
			}
		}
	}
}
