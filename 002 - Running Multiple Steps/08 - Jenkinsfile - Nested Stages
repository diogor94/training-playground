pipeline {
    agent none
     stages {
	stage ("nested") {
		agent { docker 'maven:latest' }
		steps {
			sh "mvn --version"
		}
		stages {
			stage ("Stage 2") {
				agent { docker 'node:latest' }
				steps {
					sh "npm --version"
				}
			}
		}
	}
    }
}
