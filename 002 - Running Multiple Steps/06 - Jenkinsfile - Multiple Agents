pipeline {
    agent none 
	 stages {
		stage ("Stage 1") {
			agent { docker 'alpine:latest' } 
			steps {
				sh "mvn --version"
			}
		}
		stage ("Stage 2") {
			agent { docker 'alpine:latest' } 
			steps {
				sh "npm --version"
			}
		}
	}
}
