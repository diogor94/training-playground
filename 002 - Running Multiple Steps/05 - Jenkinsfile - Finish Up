pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
                def result = sh "exit \$((RANDOM % 2))"
				sh 'echo "${result}'
            }
        }
    }
}
