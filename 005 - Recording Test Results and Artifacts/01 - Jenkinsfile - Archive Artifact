pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
                sh "curl -o file.html www.google.com"
				archiveArtifacts artifacts: 'file.html'
            }
        }
    }
}
